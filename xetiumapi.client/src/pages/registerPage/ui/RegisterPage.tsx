import {RegisterForm} from '@/widgets/RegisterForm'

export const RegisterPage = (): JSX.Element  => {
    return (
        <div>
            <RegisterForm/>
            registerPage
        </div>
    );
};

